import styled from 'styled-components';

export const NavWrapper = styled.nav`
    padding: 1rem 3rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    height: 3rem;
    position: fixed;
    top: 0;
    left: 0;
    width: 90vw;
    background: #2E3332;
`;

export const IronLogo = styled.img`
    height: 3rem;
    object-fit: cover;
`;

export const Menu = styled.img`
    height: 1rem;
    object-fit: cover;
`;