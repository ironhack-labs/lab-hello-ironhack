import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; 
// can be App.js or App.jsx, but webpack read both extensions
ReactDOM.render(
  <App />,
  document.getElementById("root")
);