import React from "react";
import ReactDOM from "react-dom";
import App from "./App.js";

//Function that tells React what to render and where to render it
ReactDOM.render( 
  <App />, //é um JSX component
  document.getElementById("root")
);