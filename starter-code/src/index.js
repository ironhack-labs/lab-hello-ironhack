import React from "react";
import ReactDOM from "react-dom";
import App from "./App.js";

ReactDOM.render(
  <div>
    {/* display the App component inside the div */}
    <App />
  </div>,
  document.getElementById("root")
);
