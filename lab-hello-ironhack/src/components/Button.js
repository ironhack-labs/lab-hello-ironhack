import './Button.css'

const Button = () => {

    const buttonStyle = {
    }

    return (
        <a style={buttonStyle} href="#" className="button">
            Awesome!
        </a>
    )
}

export default Button