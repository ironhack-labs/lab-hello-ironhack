import miLogo from "./ironhack-logo.svg";

const Navbar = (props) => {
  return (
    <nav>
      <img src={miLogo} />
    </nav>
  );
};

export default Navbar;

//Crear componente
//exportarlo
//importarlo donde lo uses

//props son info
//las img son imports tmb