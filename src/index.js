import React from "react";
import ReactDOM from "react-dom";

// * Import our App.js (and all components inside that file)
import App from "./App";

// * Render our App.js
ReactDOM.render(<App />, document.getElementById("root"));

// TODO
// ! Created 5 components in total:
// * NavBar: for the 2 first logos.
// * HeroText: for the title and subtitle.
// * ReactLogo: for the logos that repeat along the header.
// * Feature: for each feature (4 in total).
// ! Created a Data base to store features information (4).
